# theme-munki-winchester

## Installation
Make sure you have Node.js installed. Then run:

```bash
$ npm install -g vsce
```

## Package

You can use vsce to easily package and publish your extensions:

```bash
$ vsce package
# myExtension.vsix generated
```
